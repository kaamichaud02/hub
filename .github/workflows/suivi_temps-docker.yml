name: Build and push Docker image

on:
  push:
    branches: [ master, main, dev ]
    paths:
      - 'suivi_temps/**'
      - '.github/workflows/suivi_temps-docker.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set image tag from branch
        id: vars
        run: |
          if [ "${{ github.ref_name }}" = "dev" ]; then
            echo "tag=suivi_temps-dev" >> "$GITHUB_OUTPUT"
          else
            echo "tag=suivi_temps-latest" >> "$GITHUB_OUTPUT"
          fi

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: ./suivi_temps
          push: true
          build-args: |
            GIT_SHA=${{ github.sha }}
          tags: |
            ghcr.io/kaamichaud02/hub:${{ steps.vars.outputs.tag }}
            ghcr.io/kaamichaud02/hub:suivi_temps-${{ github.sha }}
